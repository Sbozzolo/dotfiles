[Unit]
Description=Runs autocommit for orgs every day

[Timer]
# Time after boot (to avoid too many task at the same time)
OnBootSec=5min
# Time between exectutions
OnUnitActiveSec=24h
Unit=autocommit.service

[Install]
WantedBy=multi-user.target
